# Map Generation

`firstDocument.md` はアーカイブとして残し、このファイルを現行のMap生成仕様メモとする。

## 目的
- run中に進行するmapの生成ルールを整理する
- ノード配置、接続、出現内容、難易度カーブを定義する
- `game-loop.md` の進行仕様と `content-schema.md` のデータ定義をつなぐ

## スコープ
- normalArea と 四天王area の生成
- GhostArea の生成
- ノード情報の事前生成
- 敵や報酬の事前配置ルール

## Map全体構造
### area構成
1. normalArea
    1. row1~3 : 通常Nodeのみ
    2. row4~36 : 複合
    2. row37~40 : 四天王Nodeのみ
2. GhostArea

### 現在方針
- run開始時に必要な範囲をまとめて生成する
- 各areaは行単位で進行する
- 各行には複数ノードが存在しうる

### 要決定
<!-- - areaごとに完全事前生成するか
- 次areaだけ遅延生成するか -->
ゲーム開始時に四天王areaまで全生成
殿堂入り後、GhostArea生成

## 共通用語
### area
- map上の大きな区間
- `normalArea` `GhostArea` を持つ

### row
- 各area内の進行段
- 下から上、または開始から終点へ向かって1段ずつ進む

### node
- プレイヤーが選択して進入する1地点

### edge
- node間の接続

## 生成の流れ
1. runSeedを決定する
2. normalAreaの骨格を生成する
3. normalAreaの各node内容を生成する
4. GhostAreaを生成する
5. 保存用のmapデータに変換する

## Seed
### 役割
- 同一run内で再現可能なmap生成を行う

### 利用候補
- ノード接続
- ノード種別
- 敵編成
- mod
- gold
<!-- - ghost出現位置 -->
ghostはghostareaのみ出現

### 要決定
<!-- - runSeedだけで全決定するか
- areaごとに派生Seedを切るか -->

GhostAreaは到達時点のサーバー状態で生成

## NormalArea
### 役割
<!-- - run前半から中盤の主進行エリア
- badge収集とビルド形成を行う -->
vs NPC

### 行数
<!-- - 現在案: 約32行 -->
- 現在案: 全40行

### 含まれるノード種別
- 通常
- ジム
- パチモンセンター
- シティ/タウン
- 四天王

### 要決定
- 実際の行数
    > 40行
- 序盤/中盤/終盤の区分
    > row0 : 初期パチモン選択Node
    > row1~4 : 通常Nodeのみ
    > row5~36 : 複合（後述）
    > row37~40 : 四天王Nodeのみ

### NormalArea生成手順案
1. NodeとEdgeの生成
    1. Node0として初期パチモン選択Nodeを作成
    2. Node0のEdgeを3とする。
    3. 以降、最大Edge2、rowの最小column:3,最大column:6を基本（以降で設定されるシティ等は例外）として上手いこと36rowまで生成。計xNode固定（xは後で調整。仮で144Node）
    4. シティの生成
        1. row:7の隣り合う2つのNodeを統合してカナダシティに設定(Edge)
        2. row:14の隣り合う2つのNodeを統合してタマムシシティに設定
        3. row:21の...コガネシティ
        4. row:28の...ミナモシティ
        5. row:35の...トバリシティ
    5. パチモンセンターの生成
        1. row:36を全てパチモンセンターにする
        2. row:5以降の空いてるNodeにランダムに計x個生成
            - 連続しない
            - なるべくばらけるけど規則的になりすぎないよう上手いこと調整する
    6. ジムリーダーNodeの生成
        1. row:5以降の空いてるNodeにランダムに計20個生成
            - 連続しない
            - なるべくばらけるけど規則的になりすぎないよう上手いこと調整する
            - 「任意のクリア可能ルートのうち複数本で badge 8個以上取得可能」を複数保障したい。けど20個もあれば勝手になんとかなりそう。
    7. 残ったNodeを通常Nodeとする
    8. row:37-40に四天王Nodeを生成する

2. トレーナーとパチモンの配置
    1. ジムリーダープール(24人)からランダムな4人を四天王に設定する。
    2. ジムリーダーと四天王計24人に、得意属性が一致するパチモン(pachimonMasterにも得意属性を持たせる)をパチモンプールから3匹ずつ設定する。
    3. 作成したジムリーダーと四天王を対象Nodeにランダムに振り分ける
    4. パチモンプールの残りをランダムに各通常Nodeに振り分け、更に余ったパチモンを初期パチモン選択Nodeに配置
    5. 通常Nodeのパチモンの合計ステータスに応じて、通常トレーナーを配置（ここは厳密でなくていい）
    6. Nodeに応じてステータスと追加skill決定
        (詳細はcontent-shemaに移動する？)
        1. row毎にステータスの合計値を設定し（線形的な関数かも）
        2. 重み付けに従ってステータスをランダムに振り分ける
        3. ジムリーダー/四天王のパチモンに追加のステータス振り分け
            - 合計値と重み分けはジムリーダー/四天王が保持（ジムリーダーMaster）
        3. row毎に決められた追加skill数だけ、skill一覧からランダムに設定（重複有りの完全ランダム）

3. Modの配置
    1. 通常Mod一覧を各Nodeにランダムに振り分ける
    2. Mod効果を対象Nodeの全ポケモンに反映させる

4. 通常・ジムリーダーの報酬決定
    1. トレーナーMasterを使ってGold決定
    2. 能力報酬決定
        1. Node内のパチモンの初期スキル（2skill*3匹の6skill）からランダムに1つ決定
        2. Node内のパチモンのパッシブ（3つ）からランダムに1つ決定
        3. Modは配置されたもの
    3. ジムリーダーの場合、badge報酬を追加

### GhostArea生成手順
Ghostのindex順に1列で並べるだけ
（Ghostが増えてきたらcolumnを増やす予定だけど、ずっと先の話）

### Ghost保存時のindex管理
完走したらindexの最後に追加する

ghostAreaで死んだ場合、現在indexの位置に割り込み、以降のGhostを後ろにずらす

同時に複数のプレイヤーが死んだ場合、上記のロジックにより、先にクリアした側が後方のindexとなる

例）
ghostが0-9まで存在しているタイミングで2人のプレイヤーがghostAreaに到達すると、同じGhostAreaが生成される。
先にプレイヤーAがindex3のghostに敗北した場合、index3の位置に割り込む
次にプレイヤーBが同じghostに敗北した場合、同様にindex3の位置に割り込み、先に敗北したプレイヤーAはindex3→index4となる。


## 四天王Node
### 役割
- 殿堂入り前の高難度Node

## GhostArea
### 役割
- 殿堂入り後に挑む追加エリア
- ghostとの非同期対戦を行う

### 現在方針
- 殿堂入り後に進行する
- 完走または敗北でrun終了

### 要決定
- 行数
    > ghostの数次第
- ノード分岐の有無
    > 当分は無し。
    > ghostが増えてきたら追加調整
- 登場ghostの選出方法
    > 保存されてるghost全て
- 同一ghostの重複出現可否
    > なし

## ノード生成
### nodeが持つ情報
- nodeId
- area種別
- rowIndex
- columnIndex
- node種別
- 接続先
- 表示用簡易情報
- 解決内容

## ノード接続
### 目的
- 分岐を作りつつ、行き止まりを防ぐ

### 要決定
- 1行あたりノード数
    > 最大6
    > row0では1
    > row1以降3~6（3の行にシティができた場合2になる）
- 最小接続数
    > 1
- 最大接続数
    > row0: 3
    > row1以降: 2
    > ただしシティでは統合前の接続を全て引き継ぐ
- edge交差を許可するか
    > 許可しない
- 孤立node禁止ルール

## ノード種別の配置ルール
### 通常
- 最も基本の戦闘ノード

### ジム
- badge獲得ノード
- 節目として配置する

### パチモンセンター
- 回復ノード

### シティ
- ショップ利用ノード

### 要決定
<!-- - 各種ノードの出現頻度
- 各rowに出せるnode種別の制限
- 同種ノード連続禁止の有無
- ジム前後の救済ノード保証 -->
上述のNormalArea生成手順に従う予定。その後調整。


## ジム配置ルール
### 目的
- normalAreaの節目を作る
- badge取得ペースを安定させる

### 要決定
<!-- - ジム数
- 固定row配置か範囲抽選か
- badge属性の割り当て方法
- 同属性ジムの重複可否 -->
上述のNormalArea生成手順参照。
- badge属性の割り当て
    > 得意属性と一致させる
    > badge効果は一律して、その属性+30%（仕様上、同属性badgeを最大3つまで取得できるが、その場合はシンプルに+60%,+90%と加算する）

## ノード内容の事前生成
### 方針候補
- map生成時に全node内容を確定する
<!-- - node到達時に内容を確定する -->

### 現在メモ
- 敵や補正は事前に配置する方針が強い

### 要決定
<!-- - どこまでを事前生成するか
- 報酬候補も先に固定するか -->
NormalArea:ゲーム開始時に報酬含めて全て事前に固定する
GhostArea:殿堂入り時に全て事前に固定する

## 敵生成
上述の生成手順参照
<!-- ### 入力候補
- area種別
- rowIndex
- node種別
- runSeed

### 出力候補
- 敵パチモン3体
- スキル数補正
- mod
- 難易度補正
- 報酬gold

### 現在メモ
- 種族ランダム
- 属性重み付き配分
- 行難易度補正
- スキル数補正
- mod付与

### 要決定
- トレーナー情報を先に引くか
- 敵3体を独立抽選にするか
- 同一種の重複禁止をどう扱うか -->

## 報酬生成
上述の生成手順参照

<!-- ### 通常戦
- 能力報酬
- gold

### ジム戦
- 能力報酬
- gold
- badge

### 四天王戦 / ghost戦
- 基本報酬なし
- 全味方HP回復あり

### 要決定
- 報酬内容をnode生成時に固定するか
- 撃破敵依存で後から決定するか -->

## 難易度カーブ
### 調整対象
- 敵属性値
- 敵skill数
    > row0でskill2個（初期スキルのみ）、row36でskill9個（追加スキル7個）になるよう調整
- あとから調整しやすい構造にしておく
    - modの強さ
    - ダメージ倍率
    - ダメージ軽減倍率

### 軸
- row進行
- area補正
    > 無し rowとnode種別で管理
- ジム/四天王/補正

### 要決定
- rowごとの伸び方
- 序盤の最低保証
- 終盤の上振れ許容量

## Ghost配置
### 役割
- 後続runに過去プレイヤーを出現させる

### 要決定
上述のghostArea生成手順参照
バージョン差異は考慮しない
（保持したデータを使って現在バージョンで生成する。バージョンが変わっても以前のGhostデータを引き継ぐ）
<!-- - GhostAreaのghost選出母集団
- 出現順
- 新しいghostが前に割り込むルール
- バージョン差異の扱い -->

## 保存データとの関係
### 保存したいもの
- runSeed
- 生成済みmap
- 現在位置
- 解決済みnode状態

### 要決定
- 未到達nodeの内容を全保存するか
- seedと進行状態だけ保存して再生成するか

> runSeedで復元できるなら不要。できなそうなら全保存。

## 実装用メモ
### 最小プロトタイプ範囲
- normalAreaのみ
- 通常ノードのみ
- 固定行数
- 固定分岐数
- 敵内容は簡易生成

### 後回しでよい要素
- 四天王area
- GhostArea
- ショップ詳細
- 救済配置の細かい最適化

## TODO
- 1行あたりノード数の初期案
    > 基本的に3~6
- ジム配置の初期案
    > 5-35でランダム。多少の均等化調整。
- センター/ショップ出現頻度
    > 上述の生成手順参照
- node接続アルゴリズム
    > どういう方法が向いてそう？
- 事前生成の範囲
    > スタート時にnormalAreaを全生成
    > 殿堂入り後、ghostAreaを全生成
